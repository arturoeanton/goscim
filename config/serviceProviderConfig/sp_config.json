{
  "_comment": "Only documentationUri is read. Everything else in /ServiceProviderConfig is derived from the running code, so it cannot advertise a capability the server does not have. See docs/configuration.md.",
  "documentationUri": "https://github.com/arturoeanton/goscim/tree/main/docs"
}
